# ShopManage-SMD 🛍️📊

**ShopManage-SMD** is a simple inventory and shop management system designed for small businesses. This application does not require a backend or database; instead, it uses **local storage** to store product and sales data on the user's browser. Perfect for lightweight use or local store management! 🏪

## Features ✨

- **Product Management**: Add, update, and delete products directly in the app. 🏷️
- **Sales Tracking**: Record sales and manage transactions using local storage. 💳
- **Reporting**: Generate sales reports based on local data. 📊
- **User Authentication**: Simple user login system for access control. 🔐

## Technologies Used ⚙️

- **Frontend**: HTML, CSS, JavaScript 💻
- **Local Storage**: Store and retrieve data directly from the browser using the browser's local storage API. 🗄️

## Installation 🚀

1. Clone the repository:
   ```bash
   git clone https://github.com/shanuka-upendra/ShopManage-SMD.git
   ```
2. Navigate into the project folder:
  ```bash
cd ShopManage-SMD
```
3. Open the index.html file in your preferred browser. No server setup is required! 🌍

### Usage 🌐
Once the app is running, you can access it directly in your browser. Simply open index.html and start managing your shop. All data will be stored in your browser's local storage.


### Key Changes:
- **No backend**: Removed any mention of backend technologies.
- **No database**: Removed database-related sections and replaced them with local storage.
- **Local storage**: Emphasized that all data is stored in the browser using the local storage API, which makes the app lightweight and simple.

Let me know if you'd like to make further adjustments!
